version: '3'

services:
  payload:
    image: node:18-alpine
    ports:
      - '3000:3000'
    volumes:
      - .:/home/node/app
      - node_modules:/home/node/app/node_modules
    working_dir: /home/node/app/
    command: sh -c "yarn install && yarn dev"
    env_file:
      - .env
    networks:
      - app-network

  db:  # Changed from 'supabase' to 'db' for common naming convention
    container_name: supabase-db
    image: supabase/postgres:15.1.1.78
    healthcheck:
      test: pg_isready -U postgres -h localhost
      interval: 5s
      timeout: 5s
      retries: 10
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD} 
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    networks:
      - app-network


# Uncomment if you're still using MongoDB
#   mongo:
#     image: mongo:4.4.18
#     platform: linux/amd64
#     ports:
#       - '27017:27017'
#     command:
#       - --storageEngine=wiredTiger
#     volumes:
#       - data:/data/db
#     logging:
#       driver: none
#     networks:
#       - app-network

volumes:
  data:
  node_modules:

networks:
  app-network:
    driver: bridge
